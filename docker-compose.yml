# image not found? login using "docker login --username=abc --password=abc https://registry.domain.de"
services: 
  pheonix-Cloud-ConfigServer: 
    container_name: pheonix-Cloud-ConfigServer
    image: "praveenit219/pheonix-Cloud-ConfigServer:latest"
    network_mode: host
    ports: 
      - "10801:10801"
version: "3"