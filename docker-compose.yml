# image not found? login using "docker login --username=abc --password=abc https://registry.domain.de"
services: 
  CloudConfigServer: 
    container_name: pheonix-CloudConfigServer
    image: "praveenit219/pheonix-CloudConfigServer:latest"
    network_mode: host
    ports: 
      - "10801:10801"
version: "3"